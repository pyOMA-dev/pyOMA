# .readthedocs.yaml
# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

# Required
version: 2

# Set the OS, Python version and other tools you might need
build:
  os: ubuntu-22.04
  tools:
    python: "3.14"
    # You can also specify other tool versions:
    # nodejs: "19"
    # rust: "1.64"
    # golang: "1.19"
  # autodoc imports the PyQt6 GUI modules to introspect them; PyQt6 needs
  # these system Qt runtime libraries to import at all, even headless
  # (same packages already installed for pytest-qt in .github/workflows/*.yml)
  apt_packages:
    - libegl1
    - libgl1
    - libxkbcommon0
    - libxkbcommon-x11-0

# Build documentation in the "docs/" directory with Sphinx
sphinx:
  configuration: doc/conf.py

# Optionally build your docs in additional formats such as PDF and ePub
# formats:
#    - pdf
#    - epub

# Optional but recommended, declare the Python requirements required
# to build your documentation
# See https://docs.readthedocs.io/en/stable/guides/reproducible-builds.html
python:
   install:
   - method: pip
     path: .
     extra_requirements:
       - docs
